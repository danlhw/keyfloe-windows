node_modules/
dist/
release/
*.log
.DS_Store
.env
.env.local
.vite/
.parcel-cache/

# Whisper model cache — too large to ship in git; downloaded at install time.
resources/whisper-models/*.bin

# Local-only secrets sometimes dropped in for testing.
.anthropic_key
.openai_key
